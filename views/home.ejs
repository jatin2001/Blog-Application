<%-include("./partial/header")-%>
    <h1>Home</h1>
    <h4><%=content%></h4>
    <%posts.forEach((item)=>{%>
        <br>
        <h1><%=item.title%></h1>
    
        <p><%=item.post.substring(0,200)%>...
            <a href='/posts/<%= item.title %>'>Read More</a>
        </p>

        <%})%>
<%-include("./partial/footer")-%>